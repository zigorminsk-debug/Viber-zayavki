// Корневой build-файл. Плагин Android Gradle применяется в app/build.gradle
plugins {
    id 'com.android.application' version '8.5.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
